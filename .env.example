# Vigilora AML — Environment Variables
# Copy this to .env and fill in your values

APP_ENV=development          # development | production

# Paths (relative to project root)
MODEL_DIR=app/models
DATA_DIR=app/data

# Optional: MongoDB URI (for future persistent alert store)
# MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net/vigilora
